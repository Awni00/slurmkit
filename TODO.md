# TODO

- `doctor`: add a diagnostics command for config, SLURM binaries, and repo layout checks.
- Notification route CRUD / management UX: add interactive create/edit/delete/list flows for routes instead of only config-wizard setup.
