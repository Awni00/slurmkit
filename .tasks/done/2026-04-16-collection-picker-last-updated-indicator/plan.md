## Plan

- Update the shared collection picker helper to read `updated_at`, sort dated collections newest-first, and render picker labels as relative recency or `unknown`.
- Keep the create-new sentinel first and leave non-interactive collection outputs unchanged.
- Extend prompt helper tests to cover `updated_at` ordering, label formatting, unknown timestamps, and future-timestamp clamping.
TODO: Write complete plan generated during plan mode verbosely here.
